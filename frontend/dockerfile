# Frontend Dockerfile (React + Vite)
FROM node:18

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Build the React app
RUN npm run build

# Install serve to serve the static files
RUN npm install -g serve

# Expose frontend port (Vite uses 5173 by default)
EXPOSE 5173

# Serve the production build
CMD ["serve", "-s", "dist"]

